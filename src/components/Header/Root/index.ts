import styled from 'styled-components';
import Color from '@/helpers/constants/Color';

/**
 * Root component of Header.
 */
const Root = styled.div`
  background-color: ${Color.darkGray};
`;

export default Root;
