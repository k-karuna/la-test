import styled from 'styled-components';

const Form = styled.form`
  margin-top: 40px;
  > * {
    &:last-child {
      margin-top: 48px;
      margin-bottom: 170px;
    }
  }
`;

export default Form;
